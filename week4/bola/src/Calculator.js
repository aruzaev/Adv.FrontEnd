let Calculator = (crops) => {
    return(
        <div>
            <h2>Addition: {10 + crops.num1}</h2>
            <h2>Addition: {crops.num1 + crops.num2}</h2>
        </div>
    );
}

export default Calculator;